# Resume Handoff

## Verified frontier

- G2 walking skeleton: stable candidate with 22 original tests and independent adversarial pass.
- Scalability contracts: case families, evidence cells, persona dimensions, specialist capabilities, and population axes implemented as additive candidate core.
- Campaign manifest: statically valid `long_run_campaign.v2` and explicitly `specification_only`.
- Projection pipeline: exact case-scoped assertion grants, matter-bound persona views, capability-authority checks, lineage graph validation, truthful rendered media type, and recomputed candidate receipts.
- Deterministic verification: 93 tests currently pass, including nine hostile regression tests added after independent rejection.

## First safe command

```powershell
python campaigns\g2-scale-v1\checks\validate_manifest_contract.py campaigns\g2-scale-v1\campaign.json
```

Then run `python -m pytest -q`. Do not launch a worker from manifest validity alone.

## First blocked node

`J000` requires Lowell's affirmative baseline, budget, exact campaign revision, controller revision, adapter qualification plan, launch command, shutdown command, and state-path authorization. No such launch receipt currently exists.

## Fable integration

Classify new feedback as a G0-G2 repair, G0-G2 hardening, G3+ proposal, source proposal, or defer. Any feedback changing truth ownership, projection privacy, authority, schema, validator, runtime, source, scope, or execution mode invalidates the affected qualification and requires a campaign revision.

## Privacy and authority

Do not read `private/`, access external services, ingest public or private row-level data, use real identities, purchase legal material, claim jurisdiction correctness, render G3 documents, publish, or deploy.

